 "use client";
import { useEffect, useState } from "react";
import { Menu, X } from "lucide-react";
import { Logo } from "./Logo";
const nav=["Experience","Our Villas","Sport Facilities","About Us"];
export function Header(){const[scrolled,setScrolled]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const f=()=>setScrolled(window.scrollY>40);f();window.addEventListener("scroll",f);return()=>window.removeEventListener("scroll",f)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-500 ${scrolled?"bg-sand/90 py-4 text-charcoal shadow-sm backdrop-blur-md":"bg-transparent py-7 text-white"}`}><div className="mx-auto flex max-w-7xl items-center justify-between px-6 md:px-10"><div>{scrolled?<Logo variant="charcoal"/>:<Logo variant="white"/>}</div><nav className="hidden items-center gap-8 text-[11px] uppercase tracking-[0.16em] lg:flex">{nav.map(i=><a href={`#${i.toLowerCase().replaceAll(" ","-")}`} key={i} className="transition hover:opacity-70">{i}</a>)}<a href="#reserve" className="rounded-full border border-current px-5 py-2 transition hover:border-gold hover:bg-gold hover:text-white">Reserve Now</a><a>FR / EN</a></nav><button className="lg:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="mx-6 mt-5 rounded-2xl bg-sand p-6 text-charcoal shadow-xl lg:hidden"><div className="flex flex-col gap-5 text-sm uppercase tracking-[0.14em]">{nav.map(i=><a key={i}>{i}</a>)}<a>Reserve Now</a><a>FR / EN</a></div></div>}</header>}
